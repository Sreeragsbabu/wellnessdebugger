# Project Setup

## Prerequisites
- Node.js installed on your system

## Installation & Running

### Step 1: Start the Backend
```bash
cd backend
npm install
npm start
```

### Step 2: Start the Frontend
Open a new terminal window and run:
```bash
cd frontend
npm install
npm run dev
```

## Access the Application
- Frontend: Open your browser and navigate to the URL shown in the terminal (typically `http://localhost:3000` or `http://localhost:5173`)
- Backend: Running on `http://localhost:4000`

## Testing

### Run Backend Tests
```bash
cd backend
npm test
```

### Run Frontend Tests
```bash
cd frontend
npm test
```

### Run Tests in Watch Mode
```bash
# Backend
cd backend
npm test -- --watch

# Frontend
cd frontend
npm test
```

---

That's it! Your application should now be running. 🚀